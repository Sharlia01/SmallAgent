[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -ra --strict-config --strict-markers

markers =
    unit: Tests that do not use external services
    api: FastAPI endpoint tests
    integration: Tests using databases or external services